# Pulse on curr.xyz — append to /etc/caddy/Caddyfile
# MCP binds localhost:3100 only (same pattern as api.fornax / api.paynxt)

api.pulse.curr.xyz {
	reverse_proxy 127.0.0.1:3100
}

# Optional — only if pulse-worker.service is enabled
# worker.pulse.curr.xyz {
# 	reverse_proxy 127.0.0.1:3200
# }
